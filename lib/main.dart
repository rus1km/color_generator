import 'package:color_generator/my_app.dart';
import 'package:flutter/material.dart';

/// The entry point of the Flutter application.
///
/// This function calls [runApp] with the [MyApp] widget as its argument,
/// which serves as the root of the application's widget tree.
void main() {
  runApp(const MyApp());
}
